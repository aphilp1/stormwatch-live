{
  "name": "stormwatch-cloud",
  "version": "1.0.0",
  "private": true,
  "description": "StormWatch public-web backend (Cloudflare Worker). Separate from the local mcp-server.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
