{
  "name": "stormwatch-smoketest",
  "private": true,
  "description": "Playwright smoke test for weather-alerts.html — see smoketest_layers.js.",
  "scripts": {
    "smoketest": "node smoketest_layers.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
