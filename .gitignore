# Dependencies
mcp-server/node_modules/

# Environment / secrets
.env
.env.local

# Backup files (git is the backup now)
*backup*
*BACKUP*
*PRE-GITHUB*

# Python
__pycache__/
*.pyc
*.pyo

# Scratch / test files
Test FIles/
Images_Claudi_AI/

# OS
Thumbs.db
desktop.ini
.DS_Store

# Synoptic API credentials -- never commit
synoptic_config.py

# CDS / ECMWF credentials -- never commit
.cdsapirc
*.cdsapirc
.ecmwfapirc
*.ecmwfapirc

# ERA5 NetCDF downloads -- large binary, reproduced by era5_event_pull.py
era5/*.nc

# Wyoming sounding raw HTML -- temp debugging files
wyoming_raw_*.html

# Downloaded data caches (large binary files, reproduced by scripts)
Storm_info/_herbie_cache/
Storm_info/_dem_cache/
Storm_info/hrrr_bc_cache/

# Script-generated backup snapshots (git is the backup)
*.pre_bc_pull.bak
*.pre_audit_bak
*.bak

# Alert monitor: large reproducible build inputs (kept LOCAL; the live Action
# reads only the small derived models baseline_model/composite_model/analog_library).
# Rebuild the raw archive with: python backfill_iem.py --start 2008 --end 2026
data/baseline_hourly.jsonl
data/baseline_manifest.json
data/_backfill.log

# Build logs (ephemeral run output)
Storm_info/phase_a_build.log
Storm_info/dem_build.log
Storm_info/merge_gate.log
Storm_info/regime_gate.log
Storm_info/regime_event_report.log

# Per-state baseline build log (ephemeral)
data/_state_build.log
